import { Page } from '../page';
import styled from 'styled-components';

export const OverallStatusPageWrapper = styled(Page)`
  background-color: #fafafa;
  margin-left: auto;
  margin-right: auto;
  position: relative;
`;
